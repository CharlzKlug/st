#include <iostream>
#include <cmath>

void findRoots (int a, int b, int c, int d){
  for (int i=-100; i<101; i++){
    if (a*pow(i*1.0, 3.0)+b*pow(i*1.0, 2.0)+c*i+d==0) cout<<i<<"\n";
  }
}

int main(){
  findRoots(1, -3, 0, 0);
  return 0;
}
