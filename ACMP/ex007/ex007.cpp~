#include <string>
#include <sstream>
#include <fstream>
#include <iostream>
#include <cmath>
using namespace std;

int main(){
  ifstream infile("input.txt");
  //  ofstream outfile("output.txt");
  string inputLine;
  infile>>inputLine;
  cout<<inputLine;
  return 0;
}
