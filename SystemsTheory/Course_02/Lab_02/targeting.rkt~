#lang racket

 (require math/array)
(define ma (array #[#[5 8 4 2]
             #[6 9 4 6]
             #[5 3 8 3]
             #[3 5 7 9]]))

(define (get-vert some-array)
  (array-axis-max some-array 1))

(define (make-horiz ma